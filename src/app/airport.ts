//Do I even need this class?  Thought I might need to connect to backend
export class Airport {
    constructor(
        public id: number,
        public name: string,
        public code: string
    ) { }
}
